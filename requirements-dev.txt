# Додаткові залежності лише для локальних тестових скриптів (scripts/), не для
# продакшн-сервісу — не імпортуються ніде в main.py / api / services.
-r requirements.txt
soundfile==0.14.0
numpy==2.5.1
